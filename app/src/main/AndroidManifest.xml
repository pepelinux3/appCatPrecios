<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.adrprecios">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar">
        <activity android:name="adr.precios.adrprecios.ExistenciaActivity"
            android:label="Existencia por Sucursal"
            android:theme="@style/Theme.AppCompat.Dialog.Alert">
        </activity>
        <activity android:name="adr.precios.adrprecios.PreciosFullActivity" />
        <activity android:name="adr.precios.adrprecios.ImagenActivity" />
        <activity android:name="adr.precios.adrprecios.PreciosActivity" />
        <activity android:name="adr.precios.adrprecios.GroupActivity" />
        <activity android:name="adr.precios.adrprecios.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

    <uses-permission android:name="android.permission.INTERNET" />

</manifest>